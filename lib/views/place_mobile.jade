extends ./_layouts/weather_mobile

block weather_content
  .page-title
    h1= title
    h2.sub-title= site.head.description
  include _includes/report.jade
  
block append scripts
  if (place.feature_class === 'P')
    script.
      UserPlaces.options.domain = '#{config.domain}';
      UserPlaces.addPlace('#{place.id}');
